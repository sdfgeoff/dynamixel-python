class Servo:
	def __init__(self, bus, id):
		"""Constructs a dynamixel servo from a python dict of registers and
		a UART bus"""
		self.data = descriptor
		
	def get_register_info(self, offset):
		"""Returns the metadata about a particular register"""
		
	def read_register(self, offset):
		"""Returns the value of a register or None"""
		
	def set_register(self, offset, value):
		"""Sets the value of a register"""
		
	def ping(self):
		"""Returns True if the ping succeeds, otherwise it returns False"""
		
	def construct(self):
		"""Reads the model number and loads the register map"""

